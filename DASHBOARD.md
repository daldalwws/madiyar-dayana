# Дашборд RSVP — Looker Studio (Google BI)

Дашборд поверх Google Таблицы с ответами анкеты: сколько людей придёт, прогресс к
лимиту **144**, динамика во времени. Бесплатно, приватно, авто-обновление.

**Подсчёт гостей:** «приду один» = 1, «приду с супругой(-ом)» = 2, «не смогу» = 0,
**плюс число детей**. Лимит 144 — общий по людям (взрослые + дети).

Таблица: лист **«Ответы»**, колонки
`A Дата ответа · B Имя · C Фамилия · D Присутствие · E Код · F Дети · G Кол-во детей · H Пожелания · I Язык`.
Форма пишет только A–I, столбец **J свободен** под формулу.

---

## Шаг 0 (рекомендуется) — колонка «Гостей» в таблице

Делает Looker простым (там будет просто `SUM(Гостей)`) и показывает счёт прямо в таблице.

В листе «Ответы»:
- **J1** напишите: `Гостей`
- **J2** вставьте формулу (тянется автоматически на новые строки):

```
=ARRAYFORMULA(IF(A2:A="";; (IF(E2:E="couple";2;IF(E2:E="alone";1;0))) + IF(G2:G="";0;N(G2:G))))
```

> Разделитель аргументов `;` — для локали KZ/RU. Если таблица в англ. локали и формула
> ругается, замените `;` на `,`.

Бонус — мини-сводка в любых свободных клетках (например L1/L2):
```
=SUM(J2:J)        // всего гостей
=144-SUM(J2:J)    // осталось мест
```

---

## Шаг 1 — подключить данные в Looker Studio

1. Открыть https://lookerstudio.google.com
2. **Create → Report** (или Blank Report).
3. **Add data → Google Sheets** → выбрать вашу таблицу → лист **«Ответы»**
   → галочка **Use first row as headers** → **Add**.

---

## Шаг 2 — метрика числа гостей

**Если сделали столбец J** — поле `Гостей` уже есть. Тип = Number, агрегация = **Sum**.

**Если НЕ делали столбец J** — создайте calculated field (Add a field / Add dimension → Create field),
имя `Гостей`, формула:

```
CASE WHEN Код = "couple" THEN 2 WHEN Код = "alone" THEN 1 ELSE 0 END
+ IFNULL(CAST(Кол-во детей AS NUMBER), 0)
```

---

## Шаг 3 — собрать виджеты (Insert → …)

| Виджет | Метрика / настройка | Зачем |
|---|---|---|
| **Scorecard** | `SUM(Гостей)` | Придёт гостей (всего) |
| **Gauge** (датчик) | `SUM(Гостей)`, Min 0, **Max 144** | Прогресс к лимиту 144 |
| **Scorecard** | поле `144 - SUM(Гостей)` | Осталось мест |
| **Scorecard** | Record Count | Сколько ответов |
| **Scorecard** | `SUM(Кол-во детей)` | Детей |
| **Scorecard** | Record Count + фильтр `Код = no` | Сколько «не смогут» |
| **Pie / Bar chart** | Dimension `Присутствие`, Metric Record Count | Разбивка: один / с парой / не смогу |
| **Time series** | Dimension `Дата ответа` (по дням), Metric `SUM(Гостей)` и Record Count | Динамика во времени |
| **Table** | `Имя, Фамилия, Присутствие, Кол-во детей, Дата ответа` | Список гостей |

«Осталось мест»: создайте calculated field с формулой `144 - SUM(Гостей)` (или текст-блок
рядом со scorecard).

Можно добавить **Filter control** (фильтр по `Присутствие` или `Язык`) сверху дашборда.

---

## Шаг 4 — свежесть данных и доступ

- **Свежесть:** Resource → Manage added data sources → Edit → **Data freshness → 15 минут**
  (Looker кэширует Google Sheets; так новые ответы подтягиваются быстрее).
- **Доступ:** кнопка **Share** — оставьте приватным (только вы) или дайте «Viewer» по ссылке
  жениху/невесте. Данные с именами гостей не делайте публичными.

Дашборд читает ту же таблицу → новые ответы анкеты появляются автоматически (после
обновления кэша).

---

## Замечания

- **Дубли:** если гость отправит анкету дважды — посчитается дважды. Решение: удалить
  лишние строки в таблице, либо в Looker добавить фильтр/дедуп по `Имя`+`Фамилия`.
- **Проверка:** `SUM(Гостей)` должен совпасть с ручной суммой по строкам
  (couple→2, alone→1, no→0, +дети). Gauge должен упираться в 144.
